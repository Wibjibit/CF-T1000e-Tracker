# Build output
dist/
.astro/

# Wrangler local state (local D1, dev secrets cache, dry-run bundles)
.wrangler/

# Dependencies
node_modules/

# Local secrets — in production these are Cloudflare Workers Secrets
# (TTN basic auth, TOTP/cookie secrets, BLOB_ENC_KEY). .dev.vars.example is the
# committed template.
.dev.vars

# Generated by scripts/import-google-account.mjs: SQL that embeds the REAL
# AES-GCM-encrypted Google credential blob. Never commit it.
scripts/out/
